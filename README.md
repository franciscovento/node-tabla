# Info:

```
Options:
      --help     Show help                                             [boolean]
      --version  Show version number                                   [boolean]
  -b, --base     define la base para la tabla                [number] [required]
  -l, --listar   Si se desea o no listar el resultado [boolean] [default: false]
  -h, --hasta    limite para tabla                        [number] [default: 10]

```
